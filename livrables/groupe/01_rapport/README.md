# Rapport de groupe

Source de verite du rapport final.

Fichiers attendus:

- `csm_rapport_groupe.tex`: source principale LaTeX
- `csm_rapport_groupe.pdf`: rendu compile a deposer et partager
- `build_report.ps1`: script local de compilation

Compilation:

```powershell
.\build_report.ps1
```
